/**
 * @deprecated Import from `src/features/callkit` instead.
 * Kept for backward compatibility with existing imports.
 */
export {
  setupCallKeepPlatform as setupCallKeep,
  startNativeCallUI as startCallUI,
  endNativeCallUI as endCallUI,
  isCallKeepReady,
} from "../features/callkit";
